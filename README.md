# Data

Place `GoldPrice.csv` in this folder before running the analysis.

Required columns:

-   `Year`
-   `Month`
-   `GoldPrice`

The original coursework dataset is intentionally not bundled in this
public-ready repository because redistribution permission has not been
established.
